<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/top_purple</item>
        <item name="colorPrimaryDark">@color/top_purple_dark</item>
        <item name="colorAccent">@color/top_red_accent</item>
    </style>

    <style name="MainTheme" parent="AppTheme">
    </style>

    <style name="TabLayoutText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!--Fragment 主题-->
    <style name="FragmentTheme" parent="AppTheme">
        <item name="android:windowBackground">@color/common_white</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
    </style>


</resources>
