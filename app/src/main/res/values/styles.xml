<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:fitsSystemWindows">true</item>
        <item name="colorPrimary">@color/top_blue</item>
        <item name="colorPrimaryDark">@color/top_blue_dark</item>
        <item name="colorAccent">@color/top_blue</item>
    </style>

    <style name="MainActivityTheme" parent="AppTheme">
    </style>

    <!--Fragment 主题-->
    <style name="FragmentTheme" parent="AppTheme">
        <item name="android:windowBackground">@color/common_white</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
    </style>


</resources>
